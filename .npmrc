install-links=false
workspaces=false
# sfmc-dataloader lists `mcdev` as an optionalDependency for full mcdev-project
# integration. This desktop app only uses the standalone .mcdatarc.json layout,
# so we omit optional deps to avoid bundling ~300 extra (and vulnerable) packages
# into the shipped installer.
omit=optional
