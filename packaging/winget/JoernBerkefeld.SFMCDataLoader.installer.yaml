# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.12.0.schema.json
# Installer manifest. `wingetcreate update` (see the release workflow) rewrites
# PackageVersion, InstallerUrl and InstallerSha256 for each new release, so the
# placeholder hash below only matters for the very first manual submission.
PackageIdentifier: JoernBerkefeld.SFMCDataLoader
PackageVersion: 0.3.0
InstallerLocale: en-US
InstallerType: nullsoft
Scope: user
InstallModes:
    - interactive
    - silent
    - silentWithProgress
UpgradeBehavior: install
Installers:
    - Architecture: x64
      InstallerUrl: https://github.com/JoernBerkefeld/sfmc-dataloader-app/releases/download/v0.3.0/sfmc-dataloader-app-0.3.0-win-x64.exe
      InstallerSha256: 11D7F19222F6F1E33293FFFA21BEF63C20C1E2A25EB1D6F4A4BBB766A99833E3
ManifestType: installer
ManifestVersion: 1.12.0
